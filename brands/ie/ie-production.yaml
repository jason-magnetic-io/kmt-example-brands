computed-services:
- name: sava-product
  version: 2.0.1
- name: sava-cart
  version: 1.0.5
environment:
  name: ie-production
services:
- name: sava-product
  vamp:
    gateway:
      policy: basic
      selector:
        discriminator: version
        type: label
    name: <name>
  version: ^1.0
- name: sava-cart
  vamp:
    gateway:
      policy: basic
      selector:
        discriminator: version
        type: label
    name: <name>
  version: ^1.0
updated: false
